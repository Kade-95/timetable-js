import { IGroup } from "../models/group.interface";
import { Timetable } from "./timetable.class";
export declare function used(timeTable: Timetable, groups: IGroup[]): string;
export declare function nully(timeTable: Timetable): string;
